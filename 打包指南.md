# 📱 打包成APP指南

本文档详细说明如何将"我的暑假计划表"打包成iOS和Android应用。

## 🎯 准备工作

### 系统要求

**iOS开发：**
- macOS系统
- Xcode（从App Store下载）
- Apple开发者账号（可选，用于发布到App Store）

**Android开发：**
- macOS、Windows或Linux
- Android Studio
- Java Development Kit (JDK) 11或更高版本

## 📦 方法一：使用Capacitor（推荐）

Capacitor是由Ionic团队开发的跨平台应用运行时，可以轻松将Web应用打包成原生应用。

### 步骤1：安装Capacitor

```bash
npm run capacitor:init
```

这个命令会：
1. 安装Capacitor核心库和CLI
2. 安装iOS和Android平台支持
3. 初始化Capacitor配置

在初始化过程中，会询问：
- **App name**: 输入 `我的暑假计划表`
- **App ID**: 输入 `com.schedule.summer`（或你自己的包名）

### 步骤2：构建Web应用

```bash
npm run build
```

这会在`dist`目录下生成优化后的生产版本。

### 步骤3：添加平台

**添加iOS平台：**
```bash
npm run capacitor:add:ios
```

**添加Android平台：**
```bash
npm run capacitor:add:android
```

### 步骤4：打开原生项目

**iOS：**
```bash
npm run capacitor:open:ios
```
这会在Xcode中打开iOS项目。

**Android：**
```bash
npm run capacitor:open:android
```
这会在Android Studio中打开Android项目。

### 步骤5：在原生IDE中构建

**iOS（在Xcode中）：**
1. 选择一个模拟器或连接真机
2. 点击运行按钮（▶️）
3. 应用会安装并运行在设备上

**Android（在Android Studio中）：**
1. 选择一个模拟器或连接真机
2. 点击运行按钮（▶️）
3. 应用会安装并运行在设备上

### 步骤6：更新应用（当你修改了代码后）

每次修改代码后，需要同步到原生项目：

```bash
npm run capacitor:sync
```

或者使用快捷命令：

```bash
# iOS
npm run app:build:ios

# Android
npm run app:build:android
```

## 📦 方法二：使用Cordova

### 步骤1：安装Cordova

```bash
npm install -g cordova
```

### 步骤2：创建Cordova项目

```bash
cordova create myapp com.schedule.summer 我的暑假计划表
cd myapp
```

### 步骤3：添加平台

```bash
cordova platform add ios
cordova platform add android
```

### 步骤4：构建并复制文件

```bash
# 在原项目目录下构建
npm run build

# 复制dist文件夹内容到Cordova项目的www目录
cp -r dist/* myapp/www/
```

### 步骤5：构建应用

```bash
# iOS
cordova build ios

# Android
cordova build android
```

### 步骤6：运行应用

```bash
# iOS
cordova run ios

# Android
cordova run android
```

## 📦 方法三：PWA（最简单）

如果不需要原生功能，可以将应用部署为PWA（渐进式Web应用）。

### 步骤1：添加manifest.json

在`public`目录下创建`manifest.json`：

```json
{
  "name": "我的暑假计划表",
  "short_name": "暑假计划",
  "description": "专为小学生设计的暑假作息学习计划表",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#FFF5F7",
  "theme_color": "#FF6B9D",
  "icons": [
    {
      "src": "/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

### 步骤2：在index.html中引用

在`<head>`标签中添加：

```html
<link rel="manifest" href="/manifest.json">
<meta name="theme-color" content="#FF6B9D">
```

### 步骤3：部署到服务器

将构建后的`dist`目录部署到任何Web服务器（如Netlify、Vercel、GitHub Pages等）。

### 步骤4：添加到主屏幕

用户在手机浏览器中访问应用后：
- **iOS**: 点击分享按钮 → "添加到主屏幕"
- **Android**: 浏览器会提示"添加到主屏幕"

## 🎨 自定义应用图标

### 准备图标

需要准备不同尺寸的图标：
- **iOS**: 1024x1024 (App Store), 180x180, 120x120, 87x87等
- **Android**: 512x512, 192x192, 144x144, 96x96等

### 使用在线工具生成

推荐使用以下工具自动生成所有尺寸：
- [App Icon Generator](https://appicon.co/)
- [Icon Kitchen](https://icon.kitchen/)

### 替换图标

**Capacitor:**
- iOS: 替换 `ios/App/App/Assets.xcassets/AppIcon.appiconset/` 中的图标
- Android: 替换 `android/app/src/main/res/` 各个mipmap文件夹中的图标

## 🚀 发布应用

### iOS App Store

1. 在Xcode中配置签名
2. 选择 Product → Archive
3. 上传到App Store Connect
4. 填写应用信息和截图
5. 提交审核

### Google Play Store

1. 在Android Studio中生成签名的APK或AAB
2. 登录Google Play Console
3. 创建新应用
4. 上传APK/AAB
5. 填写应用信息和截图
6. 提交审核

## 🔧 常见问题

### Q: Capacitor和Cordova有什么区别？
A: Capacitor是更现代的解决方案，性能更好，更容易配置。推荐使用Capacitor。

### Q: 我没有Mac，可以开发iOS应用吗？
A: 不可以。iOS应用必须在macOS上使用Xcode构建。但你可以先开发Android版本。

### Q: 应用很大，如何优化？
A: 
- 使用`npm run build`构建生产版本
- 压缩图片资源
- 移除不必要的依赖

### Q: 如何调试应用？
A: 
- **iOS**: 在Xcode中使用Safari开发者工具
- **Android**: 在Chrome中使用`chrome://inspect`

### Q: 应用需要网络吗？
A: 不需要！所有数据都存储在本地，完全离线可用。

## 📚 更多资源

- [Capacitor官方文档](https://capacitorjs.com/docs)
- [Cordova官方文档](https://cordova.apache.org/docs/en/latest/)
- [iOS开发指南](https://developer.apple.com/documentation/)
- [Android开发指南](https://developer.android.com/docs)

---

**祝你打包顺利！如有问题，欢迎查阅官方文档或寻求帮助。** 🎉
